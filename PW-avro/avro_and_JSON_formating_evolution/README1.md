# Avro Reader

## Install dependencies

Install the required packages:

pip install -r requirements.txt

## Run the program

Run the script with:

python read_avro.py

The program reads the `example.avro` file, prints the content in JSON format, and saves the result in `out.json`.